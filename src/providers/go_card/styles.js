import { StyleSheet } from 'react-native'

const providerHeight = 20
const providerWidth = providerHeight * 5.17

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#00457c',
  },
  cardLogo: {
    height: 50,
    width: 77.8,
  },
  providerLogo: {
    height: providerHeight,
    width: providerWidth,
  },
})

export default styles
